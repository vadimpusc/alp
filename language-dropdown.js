document.addEventListener('DOMContentLoaded',()=>{const select=document.querySelector('#lang');if(!select)return;const wrap=select.closest('label'),names={it:'Italiano',en:'English',uk:'Українська'};wrap.classList.add('lang-control');select.classList.add('native-lang');const trigger=document.createElement('button');trigger.type='button';trigger.className='lang-trigger';trigger.setAttribute('aria-haspopup','listbox');trigger.setAttribute('aria-expanded','false');const menu=document.createElement('div');menu.className='lang-menu';menu.setAttribute('role','listbox');function render(){trigger.innerHTML=`<span>${names[select.value]}</span><i aria-hidden="true"></i>`;menu.querySelectorAll('button').forEach(b=>{const active=b.dataset.lang===select.value;b.classList.toggle('active',active);b.setAttribute('aria-selected',active)})}Object.entries(names).forEach(([code,name])=>{const b=document.createElement('button');b.type='button';b.dataset.lang=code;b.setAttribute('role','option');b.innerHTML=`<span>${name}</span><small>${code==='uk'?'UA':code.toUpperCase()}</small>`;b.onclick=()=>{select.value=code;select.dispatchEvent(new Event('change',{bubbles:true}));wrap.classList.remove('open');trigger.setAttribute('aria-expanded','false');render();trigger.focus()};menu.appendChild(b)});trigger.onclick=()=>{const open=wrap.classList.toggle('open');trigger.setAttribute('aria-expanded',open);if(open)menu.querySelector('.active').focus()};wrap.append(trigger,menu);select.addEventListener('change',render);document.addEventListener('click',e=>{if(!wrap.contains(e.target)){wrap.classList.remove('open');trigger.setAttribute('aria-expanded','false')}});wrap.addEventListener('keydown',e=>{if(e.key==='Escape'){wrap.classList.remove('open');trigger.setAttribute('aria-expanded','false');trigger.focus()}});render()});
